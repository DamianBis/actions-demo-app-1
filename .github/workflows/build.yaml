on:
  push:
    branches: ['main']

jobs:
  do-build:
    permissions: write-all
    uses: DamianBis/actions-demo-lib/.github/workflows/buildImage.yml@main
    with:
      repo: ${{ github.repository }}
    secrets:
      envPAT: ${{ secrets.GITHUB_TOKEN }}